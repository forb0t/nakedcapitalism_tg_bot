# Интеграция Telegraph с Naked Capitalism Bot

## Обзор

Интеграция позволяет автоматически публиковать статьи из блога Naked Capitalism в Telegraph. Все статьи сохраняются в базе данных с возможностью последующей публикации в Telegraph.

## Функции

### 1. Ручная публикация

#### Публикация одной статьи
```
/publish [id]
```
Опубликовать конкретную статью по её ID из базы данных.

**Пример:**
```
/publish 1
```

#### Публикация всех неопубликованных статей
```
/publish_all [limit]
```
Опубликовать все неопубликованные статьи (или первые N статей, если указан limit).

**Примеры:**
```
/publish_all      # Опубликует первые 10 неопубликованных статей
/publish_all 5    # Опубликует первые 5 неопубликованных статей
/publish_all 20   # Опубликует первые 20 неопубликованных статей
```

### 2. Статистика и просмотр

#### Статистика публикаций
```
/telegraph_stats
```
Показывает:
- Всего статей в базе
- Количество опубликованных в Telegraph
- Количество неопубликованных
- Процент публикации

#### Последние опубликованные статьи
```
/telegraph_latest [limit]
```
Показывает список последних опубликованных статей с ссылками на Telegraph.

**Примеры:**
```
/telegraph_latest      # Покажет 10 последних
/telegraph_latest 5    # Покажет 5 последних
```

### 3. Автоматическая публикация

В боте можно включить автоматическую публикацию новых статей. При обнаружении новых статей они автоматически будут опубликованы в Telegraph.

```python
# В коде бота
bot = IntegratedNakedCapBot(
    bot_token="YOUR_TOKEN",
    telegraph_token="OPTIONAL_TOKEN",
    auto_publish=True  # Включить автоматическую публикацию
)
```

## Структура базы данных

База данных `articles.db` была автоматически обновлена следующими полями:

- `telegraph_url` - URL опубликованной статьи в Telegraph
- `telegraph_path` - Путь статьи в Telegraph (например, "Article-Title-12-15")
- `telegraph_published_at` - Время публикации в Telegraph

## Как это работает

### Процесс публикации

1. **Извлечение контента**
   - Используется `ArticleProcessor` для получения полного контента статьи с сайта
   - Контент очищается от рекламы и ненужных элементов

2. **Форматирование**
   - Контент форматируется в формат Telegraph Node
   - Добавляется заголовок, информация об авторе
   - Добавляется ссылка на оригинальную статью

3. **Публикация**
   - Статья публикуется через Telegraph API
   - Получается URL опубликованной статьи

4. **Сохранение**
   - URL и метаданные сохраняются в базу данных
   - Статья помечается как опубликованная

### Формат статей в Telegraph

Каждая статья в Telegraph содержит:

1. **Заголовок** (h3)
2. **Информация об авторе** (курсив)
3. **Разделитель** (hr)
4. **Контент статьи** (параграфы)
5. **Разделитель** (hr)
6. **Ссылка на оригинал** с эмодзи 📰

## Использование без бота

Вы можете использовать `telegraph_article_converter.py` напрямую:

```python
from telegraph_article_converter import TelegraphArticleConverter

# Создаем конвертер
converter = TelegraphArticleConverter(telegraph_token="OPTIONAL_TOKEN")

# Получаем статистику
stats = converter.get_statistics()
print(f"Опубликовано: {stats['published_telegraph']}/{stats['total_articles']}")

# Публикуем одну статью
result = converter.publish_article_to_telegraph(article_id=1)
if result:
    print(f"Опубликовано: {result['url']}")

# Публикуем несколько статей
results = converter.publish_multiple_articles(limit=10)
for r in results:
    if r['success']:
        print(f"✓ Статья {r['article_id']}: {r['telegraph_url']}")

converter.close()
```

## Запуск примера

```bash
python telegraph_article_converter.py
```

Этот скрипт:
1. Покажет статистику
2. Опубликует первые 5 неопубликованных статей
3. Покажет результаты

## Настройка Telegraph аккаунта

При первом использовании автоматически создается аккаунт Telegraph. Токен сохраняется в объекте `TelegraphArticleConverter`.

**Важно:** Сохраните токен для повторного использования:

```python
# Получаем токен из созданного аккаунта
converter = TelegraphArticleConverter()
account_token = converter.publisher.access_token
print(f"Сохраните этот токен: {account_token}")

# Используйте токен при следующем запуске
converter = TelegraphArticleConverter(telegraph_token=account_token)
```

## Команды бота

### Основные команды
- `/start` - Главное меню
- `/help` - Справка
- `/check` - Проверить новые статьи
- `/latest` - Последние статьи
- `/stats` - Общая статистика

### Telegraph команды
- `/publish [id]` - Опубликовать статью
- `/publish_all [limit]` - Опубликовать все неопубликованные
- `/telegraph_stats` - Статистика Telegraph
- `/telegraph_latest [limit]` - Последние опубликованные

## Интерфейс бота

Бот имеет удобное меню с кнопками:
- 📝 **Telegraph** - Меню управления публикациями
- 📊 **Статистика** - Быстрый доступ к статистике
- 📚 **Последние** - Просмотр опубликованных статей

## Обработка ошибок

Система обрабатывает следующие ситуации:
- Отсутствие контента статьи
- Ошибки API Telegraph
- Проблемы с базой данных
- Сетевые ошибки

Все ошибки логируются для отладки.

## Ограничения

- Telegraph имеет лимит контента: до 64 KB на статью
- Слишком длинные статьи могут быть обрезаны
- Рекомендуется публиковать не более 10-20 статей за раз для избежания rate limiting

## Расширенные возможности

### Публикация по расписанию

Вы можете настроить автоматическую публикацию по расписанию, используя `schedule`:

```python
import schedule
import time
from telegraph_article_converter import TelegraphArticleConverter

converter = TelegraphArticleConverter()

def publish_new_articles():
    """Публикация новых неопубликованных статей"""
    results = converter.publish_multiple_articles(limit=5)
    print(f"Опубликовано: {sum(1 for r in results if r['success'])} статей")

# Публикация каждый час
schedule.every().hour.do(publish_new_articles)

while True:
    schedule.run_pending()
    time.sleep(60)
```

### Интеграция с мониторингом

Статьи автоматически добавляются в базу при мониторинге. Вы можете настроить автоматическую публикацию:

```python
# В article_monitor.py или integrated_nakedcap_bot.py
def notify_new_articles(self, articles):
    """После обнаружения новых статей"""
    # Публикуем в Telegraph
    converter = TelegraphArticleConverter()
    # ... публикация статей
```

## Поддержка

При возникновении проблем:
1. Проверьте логи (`nakedcap_monitor.log`)
2. Убедитесь, что база данных обновлена
3. Проверьте доступность Telegraph API
4. Убедитесь, что токен Telegraph валиден

